name: Build Azizi Grocery Android App (APK)

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Capacitor 8 requires Node.js 22+.
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      # package-lock.json in the repository is older than package.json.
      # Use npm install so CI reconciles the lockfile instead of failing at npm ci.
      - name: Install NPM Dependencies
        run: npm install --no-audit --no-fund

      - name: Run Type Check & Verification
        run: npm run lint

      - name: Build Web Application
        run: npm run build

      # Regenerate the Android web assets/config from the current web build.
      - name: Sync Capacitor Android
        run: npx cap sync android

      # Capacitor 8 / current Android Gradle configuration requires JDK 21.
      - name: Setup Java JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # The repository does not contain android/gradlew, so use a pinned Gradle
      # distribution directly instead of failing on chmod ./gradlew.
      - name: Setup Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Verify Android Build Environment
        run: |
          node --version
          java -version
          gradle --version

      - name: Build Android Debug APK
        run: gradle -p android assembleDebug --no-daemon --stacktrace

      - name: Prepare APK Artifact
        run: |
          mkdir -p build-artifacts
          cp android/app/build/outputs/apk/debug/app-debug.apk build-artifacts/Azizi-POS-Grocery.apk

      - name: Upload Android APK as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Azizi-POS-Grocery-APK
          path: build-artifacts/Azizi-POS-Grocery.apk
          if-no-files-found: error
          retention-days: 30

      - name: Create or Update GitHub Release for Mobile Direct Download
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        continue-on-error: true
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest-apk
          name: "أحدث إصدار - تطبيق بقالة العزي (APK)"
          body: |
            📱 **تطبيق بقالة العزي للمواد الغذائية - للأندرويد**

            ملف APK مبني تلقائياً من الفرع الرئيسي.
          files: build-artifacts/Azizi-POS-Grocery.apk
          prerelease: false
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
